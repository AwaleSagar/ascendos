name: markdownlint

on:
  pull_request:
    paths:
      - "**/*.md"
      - ".github/workflows/markdownlint.yml"
      - ".markdownlint-cli2.yaml"
  push:
    branches: [main]
    paths:
      - "**/*.md"
      - ".github/workflows/markdownlint.yml"
      - ".markdownlint-cli2.yaml"

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # The action reads rules and ignores from .markdownlint-cli2.yaml; the
      # globs input below selects which files to lint.
      - uses: DavidAnson/markdownlint-cli2-action@v19
        with:
          globs: "**/*.md"
